kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080   # 暴露 NodePort 到宿主机
        hostPort: 8080
      - containerPort: 30000
        hostPort: 3000
  - role: worker
  - role: worker